/**
 * Create a variable that stores the value 'Hello World!'. 
	Output each character on a new line using a for-loop.
 */

var str = 'Hello World!'

for(let i in str)
    console.log(str[i])
//console.log(str.split("").join("\n"))